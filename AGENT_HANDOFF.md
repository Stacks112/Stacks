# AGENT_HANDOFF — 이 파일은 이동했다 (moved)

**실제 인계장은 private 저장소로 옮겼다 (2026-08-13, june 결정).**

## 새 위치

```
Stacks112/Stacks-internal  →  AGENT_HANDOFF.md
```

같은 저장소로 함께 옮긴 것: `CLAUDE.md`, `AGENTS.md`, `STACKS_CONTEXT.md`,
`worker/` (Cloudflare Worker 소스), `scripts/scout.py`,
`.github/workflows/deploy-worker.yml`.

## 에이전트 세션이 지켜야 할 것

1. 작업을 시작하기 전에 **`Stacks112/Stacks-internal` 의 `AGENT_HANDOFF.md`** 를 읽는다.
2. 작업을 마치면 **그쪽 파일**에 인계 항목을 추가한다. **이 공개 저장소의 이 파일에는 쓰지 않는다.**
3. 저장소 매니페스트(`CLAUDE.md`)도 그쪽에 있다. Cowork 세션은 접근이 안 되면
   claude.ai 프로젝트 문서 `claude/CLAUDE-md-mirror.md` 사본을 읽는다.
4. 접근 권한이 없으면 june 에게 요청한다. **권한이 없다고 해서 이 공개 저장소에 인계를 기록하지 않는다.**

## 이 공개 저장소에 그대로 남아 있는 것

사이트와 발행 파이프라인은 전부 여기 있다 — `items.json`, `index.html`, `assets/`,
`data/`, `p/`, `e/`, `og/`, `scripts/` (scout.py 제외), `.github/workflows/` (deploy-worker.yml·
draft-cards.yml 제외). 발행 절차는 바뀌지 않았다.

## Why this file is a stub

The real handoff log moved to the private repository `Stacks112/Stacks-internal`.
Read and update `AGENT_HANDOFF.md` there, not here. Ask june for access if you cannot reach it.
