# 플레이북 2 — 운영·배포·세션 규칙 (Stacks에서 증류)

> Stacks 운영 중 실제 사고와 그 재발 방지 장치에서 나온 규칙 모음.
> 대부분 "Claude 세션 여러 개 + 자동화 봇이 같은 레포에 쓰는" 환경에서 생긴 문제들이라,
> 새 서비스도 같은 방식으로 개발한다면 그대로 가져갈 가치가 있다.
> 처음부터 전부 도입할 필요는 없다 — §7의 "언제 무엇을 도입하나" 참조.

## 1. 세션 인계 체계 (첫 주에 만들 것)

Stacks에서 가장 수익이 컸던 장치. 2026-07-19에 "이미 있는 걸 다시 만들고 덮어쓰는" 사고를
겪은 뒤 생겼다.

- **`claude/START-HERE.md`** (파일명 영구 고정) — 새 세션이 무조건 먼저 읽는 진입점.
  읽기 순서, 절대 규칙, 현재 스냅샷 포인터만 담는다. **상태는 여기 쓰지 않는다**
  (규칙이 바뀔 때만 수정).
- **레포 `CLAUDE.md` = 단일 진실 출처.** 아키텍처·워크플로·스크립트의 정답.
  프로젝트 문서와 다르면 레포가 이긴다. 아키텍처가 바뀐 세션은 같은 흐름에서 CLAUDE.md도 갱신.
- **상태 문서**: 세션이 끝날 때 `claude/status-YYYY-MM-DD-<주제>.md`로 한 일·남은 일·커밋
  해시·기한 있는 액션을 기록. **파일명은 세션마다 고유하게** — 두 세션이 같은 경로에 써서
  서로 덮어쓴 사고가 실제로 있었다 (프로젝트 문서는 patch가 없고 통째 덮어쓰기라서).
- 이전 문서가 오진을 담고 있었으면 조용히 덮지 말고 **정정 표시**를 남긴다.
- 코드를 바꿀 파일은 **라이브 raw로 직접 읽고** 시작한다. 프로젝트 안의 코드 미러는 낡았을
  수 있다.

세션 종료 프로토콜: ① CLAUDE.md 갱신(아키텍처 변경 시) ② status 문서 저장 ③ 오진 정정
④ 잡은 락 해제.

## 2. 동시 작업 규칙 — "경로당 작성자 하나"

충돌의 원인은 동시성 자체가 아니라 **같은 파일을 통째로 교체하는 배포 방식**이다.
git이 충돌을 알려줄 기회 없이 조용히 덮인다. 그래서:

- **경로당 작성자 하나.** 경로가 다르면 여러 창이 동시에 작업해도 안전하다.
  (예: 백엔드 worker/ · 프론트 index.html+assets/ · 콘텐츠 문서 · 자동화 scripts/ = 4개 레인)
- 콘텐츠 데이터 파일(items.json류)은 자동화 루틴 전용 — 사람·세션은 손대지 않는다
  ("손발행 금지", 장애 시 예외 + 사후 기록).
- 생성물(정적 페이지·OG 에셋)은 Actions 전용.
- **`claude/WORK-LOCK.md` 락 보드**: 쓰기 전에 행 추가, 끝나면 삭제. 협조적(advisory) 락이라
  완벽하지 않지만 수십 분~수 시간 작업 단위에는 충분하다.

## 3. 덮어쓰기 방어 2겹 (락을 안 읽는 상대까지 대비)

락은 상대가 읽어야 작동한다 — 안 읽는 창이 실제로 있었고, 배포 8분 전 다른 세션 커밋을
이 장치가 잡아낸 실전 사례가 있다.

- **1겹 `scripts/deploy_guard.py` (업로드 전 예방)**: 내 베이스와 origin/main을 비교해 올릴
  파일이 그 사이 움직였는지 검사. 움직였으면 종료코드 2 + rebase 안내.
  rebase 후에는 `--verify`로 **상대 커밋의 줄이 지금 파일 바이트에 실제로 남아 있는지** 대조
  (rebase 성공은 보존의 증거가 아니다 — 올라가는 건 rebase 결과가 아니라 파일 바이트).
- **2겹 `.github/workflows/clobber-guard.yml` (푸시 후 탐지)**: main 푸시마다 "이 커밋이 지운
  줄 중 최근 24시간 내 다른 커밋이 추가한 줄이 있는가"를 검사 → 걸리면 실행 실패 + 이슈 자동
  생성. 오탐 억제가 핵심(공백·짧은 줄 제외, 3줄 미만 무시, 봇 커밋 제외, 의도된 재작성은
  커밋 메시지 `[clobber-ok]`). **울부짖는 알람은 꺼진다.**

두 스크립트 모두 Stacks 레포(`Stacks112/Stacks`)의 `scripts/`와 `.github/workflows/`에 있다.
새 레포에 거의 그대로 복사해 쓸 수 있다 (봇 계정명 목록만 수정).

## 4. 패치·배포 요령

- **로컬 편집 스크립트는 앵커 문자열 치환으로, 전송 명세는 줄 번호가 아니라 앵커로.**
  앵커 기반이면 베이스가 움직여도 `git reset --hard origin/main` 후 재적용이 1분에 끝난다.
  줄 번호 기반이었으면 전부 손으로 다시 맞춰야 했다.
- **베이스가 자주 움직이는 레포에서 배포는 한 호흡에** (패치 생성 → 검증 → 커밋).
  자동화 봇이 3시간마다 커밋하는 환경에서는 배포 도중에도 베이스가 움직인다.
- 자동화 회차 직전·직후 ±10분은 피해서 배포.
- **sha 대조는 선택이 아니라 필수**: 업로드 전 로컬 sha 확정 → 업로드 후 원격 raw sha 대조.
  브라우저 청크 전송에서 1글자 치환이 두 번 났고, GitHub 웹에디터는 base64를 조용히
  손상시킨 적도 있다. 큰 파일은 조각별 sha까지.
- **워크플로가 초록이어도 생성물이 최신이라는 보장은 없다.** 빌드 스크립트를 고쳤으면 생성
  페이지에서 직접 확인: `git show origin/main:<생성물 경로> | grep <새 문자열>`.
- GitHub Actions 상태는 목록 페이지 요약을 믿지 말고 **개별 run 페이지**로 확인 (캐시 지연).
- 샌드박스에서 git push가 막히는 세션이 있다 — "권한 없음"으로 포기하지 말 것. 검증된 우회:
  ① GitHub 웹 "Upload files"로 파일 업로드 커밋 (같은 디렉터리는 한 커밋에 묶임)
  ② edit 페이지의 CodeMirror 문서를 베이스로 그 자리에서 패치 (낡은 사본 업로드가 구조적으로
  불가능해지는 방식 — 클론 없는 세션에서 최선).

## 5. 베타/프로덕션 원칙

- **"베타는 실험실, 프로덕션이 항상 기준."** 베타 작업 중에도 배포 베이스는 반드시 최신
  라이브에서 새로 받는다 — 베타 작업 중 다른 세션이 프로덕션을 바꿔 stale 베이스로 덮을 뻔한
  사고에서 나온 원칙.
- 기능 플래그(예: `?beta` 게이트, `ADS_ON` 상수)로 배포와 공개를 분리하면 승인 전에도 코드를
  미리 배포해둘 수 있다.
- 검증 표준: 로컬 Playwright (모바일 390×844 + 데스크톱 1280) → JS 에러 0 확인 → 배포 →
  라이브 raw sha 대조 → 라이브 실측. 이 5단계가 Stacks의 기본 배포 검증 루틴이었다.

## 6. 자잘하지만 비쌌던 교훈들

- **naive/aware datetime 비교 버그** — "특정 소스만 수집된다"는 미스터리의 원인.
  날짜 비교는 전부 timezone-aware로 통일.
- **기본 User-Agent는 Cloudflare가 차단한다** — 자동화 스크립트의 HTTP 요청에는 브라우저 UA를.
- **정규식 경계(\b)는 JS(ASCII)와 Python(유니코드)이 다르다** — 한글 붙은 토큰 매칭 시 주의.
- CSS 공통 규칙을 직접 고치면 다른 뷰포트도 같이 바뀐다 — 미디어쿼리 안 override로.
- 이벤트 버블링: 카드 안 클릭 요소에는 `stopPropagation()` 습관화.
- localStorage에 관리자 키 같은 정적 시크릿을 두지 말 것 (XSS 한 방에 털린다).
- 성공 기준(게이트)을 세우면 **계측기를 같은 날 달 것** — 나중에 달면 초기 데이터가 없다.

## 7. 언제 무엇을 도입하나 (새 프로젝트 순서 제안)

프로토타입 단계부터 전부 갖출 필요는 없다. Stacks가 실제로 도입한 시점 기준:

1. **1일차부터**: 레포 CLAUDE.md, .gitignore, 한국어 응답 규칙, status 문서 습관
2. **첫 사고 전에 (권장: 세션 2개를 쓰기 시작할 때)**: START-HERE.md, WORK-LOCK.md,
   경로당 작성자 규칙
3. **자동화 봇이 레포에 쓰기 시작할 때**: deploy_guard.py + clobber-guard.yml 복사,
   앵커 기반 패치 습관
4. **예약 자동화가 3개를 넘을 때**: 얇은 로더 아키텍처 (규칙은 프로젝트 문서로),
   automation-map 문서 (자동화 전체 지도 1장)
5. **외부 사용자가 생기면**: 보안 점검 (플레이북 1의 §4 체크리스트), 개인정보처리방침
